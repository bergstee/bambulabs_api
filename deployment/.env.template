# Bambu Labs Printer Monitor Configuration

# Database Configuration (Required)
DB_HOST=192.168.1.236
DB_PORT=5432
DB_NAME=familyRewardDb
DB_USER=postgres
DB_PASSWORD=your_database_password

# Application Configuration
PYTHONPATH=/opt/bambulabs-monitor
PYTHONUNBUFFERED=1

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=/var/log/bambulabs-monitor/app.log

# Monitoring Settings
# Continuous monitoring interval (seconds between checks)
MONITOR_INTERVAL=10

# Status logging interval (how often to log status to database)
STATUS_LOG_INTERVAL=300

# Network timeout settings
PING_TIMEOUT=3
MQTT_TIMEOUT=10

# Retry settings for unreachable printers
RETRY_INTERVAL_SECONDS=300

# Development/Production Mode
ENVIRONMENT=production

# Optional: Override default paths
# SCRIPT_DIR=/opt/bambulabs-monitor/examples
# LOG_DIR=/var/log/bambulabs-monitor

# Note: Printer configurations should be stored in the PostgreSQL database
# in the 'printers' table with columns:
# - printer_id (primary key)
# - printer_name
# - printer_ip
# - printer_bambu_id (serial number)
# - access_code
# - ams_count (number of AMS units, 0 for non-AMS printers)